/** 
 * Operador termario.
*/
const edad = 20;
/*
if(edad >= 18) {
    console.log('Eres mayor de edad');
} else {
    console.log('Eres menor de edad');
}*/

/** Estructura de operador termario */
const mensaje = edad > 18 ? 'Eres mayor de edad' : 'Eres menor de edad'

document.write(`<P>${mensaje}</p>`);